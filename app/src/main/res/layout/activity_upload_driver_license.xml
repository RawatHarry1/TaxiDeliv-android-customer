<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="20dp"
        tools:context=".view.ui.UploadDriverLicense">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/ivBackDriverUpload"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginVertical="10dp"
            android:src="@drawable/ic_close"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvUploadDriver"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginVertical="20dp"
            android:fontFamily="@font/sufi_semi_bold"
            android:text="@string/upload_driver_s_license"
            android:textColor="@color/walk_title_color"
            android:textSize="22sp"
            android:textStyle="normal"
            app:layout_constraintTop_toBottomOf="@+id/ivBackDriverUpload" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvDocImage"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginVertical="15dp"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toTopOf="@+id/tvNextLicense"
            app:layout_constraintTop_toBottomOf="@+id/tvUploadDriver"
            tools:listitem="@layout/item_doc_image" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/tvNextLicense"
            android:layout_width="match_parent"
            android:layout_height="46dp"
            android:background="@drawable/bg_theme_shape_round_button"
            android:fontFamily="@font/poppins_regular"
            android:gravity="center"
            android:paddingVertical="7dp"
            android:text="@string/next"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="normal"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>